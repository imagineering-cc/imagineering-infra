# Radicale htpasswd users
# Copy to secrets.yaml and encrypt with: sops -e -i secrets.yaml
#
# Generate entries with: htpasswd -nB username
# Paste the full "user:$2y$..." lines here, one per line
htpasswd_users: |
  nick:$2y$05$...
